'use babel';

import GeoserverSldEditorView from '../lib/geoserver-sld-editor-view';

describe('GeoserverSldEditorView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
